# File: requirements.txt
################################################################################
# File: requirements.txt
################################################################################
# Python dependencies for Misophonia Research RAG System

# Core dependencies
openai>=1.82.0
gremlinpython>=3.7.3
aiofiles>=24.1.0
python-dotenv>=1.0.0

# Existing dependencies (likely already installed)
supabase>=2.0.0
pathlib
asyncio
argparse
logging
json
re
collections
datetime
typing
multiprocessing
textwrap
concurrent.futures
tempfile
uuid

# Optional for async progress bars
tqdm

# Data processing
pandas
numpy

# PDF processing
PyPDF2>=3.0.0
unstructured>=0.10.0
pdfminer.six>=20221105

# Utilities
colorama>=0.4.6

# For concurrent processing
concurrent-log-handler>=0.9.20

# Async dependencies for optimized pipeline
aiohttp>=3.8.0

# Token counting for OpenAI rate limiting
tiktoken>=0.5.0
